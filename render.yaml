services:
  - type: web
    name: resync-studios
    runtime: node
    plan: free
    buildCommand: npm run build:render
    startCommand: npm run start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "20"